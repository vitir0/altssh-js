{
  "name": "render-safe-proxy",
  "version": "1.0.0",
  "description": "Safe allowlisted proxy for Render.com",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "render-build": "npm ci || npm install"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "node-fetch": "^2.7.0"
  }
}
